{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Edge against localhost",
            "type": "msedge",
            "request": "launch",
            "url": "http://localhost:5500",  // Update the port if it's different
            "webRoot": "${workspaceFolder}",
        }
    ]
}
